## 📘 Описание

Сервис предназначен для **автоматического обнаружения мошеннических транзакций в банковских опреациях**.  

Решение построено как end-to-end pipeline, работающий поверх Kafka. Транзакции поступают в Kafka-топик, далее применяется сервис скоринга с предобученной моделью. И результаты записываются в postgress bd.



## ⚙️ Архитектура проекта
```
├── docker-compose.yml
├── README.md
├── test.csv # тестовый файл большого размера. Можно ограничить кол-во строк для загрзуки в настройках
│
├── fraud_database/
│   └── init.sql    # Инициализация БД PostgreSQL
│
├── fraud_detector/
│   ├── Dockerfile
│   ├── app.py              # Основной сервис: мониторинг и обработка файлов
│   ├── loader.py           # Загрузка модели и статистик
│   ├── preprocessing.py   #  Функции препроцессинга и обработки датасета
│   ├── scripts.py          # Предобработка, инференс и аналитика
│   └── requirements.txt
│
├── database_logger/
│   ├── Dockerfile
│   ├── app.py              # Kafka пишет результаты в PostgreSQL
│   └── requirements.txt
│
├── interface/
│   ├── Dockerfile
│   ├── app.py              # Streamlit UI
│   ├── requirements.txt
│   └── .streamlit/config.toml
│
├── weights/
│   ├── catboost_model.cbm  # Предобученная модель
│   ├── user_stats.csv  # Статистика по пользователям
│   └── city_stats.csv # Статистика по городам
│
```


## Модель
Модель — CatBoostClassifier (градиентный бустинг, Logloss, depth=7), обученный на числовых и категориальных признаках. Препроцессинг формирует агрегаты по пользователю (count, mean, std, min, max, активность), временные признаки (year, month, hour, is_weekend, is_night), геодистанцию между мерчантом и клиентом, логарифмы сумм и центов, длины и частоты текстовых полей, почтовые индексы (zip1, zip3), а также городские частотные фичи (city_freq, city_rareness). На валидации модель достигла ROC-AUC ≈ 0.982.


## Сборка Docker-образа

### Требования
- Docker версии 20.10+
- 2 ГБ свободного места
- Python 3.12 (включён в образ)

```bash
docker compose up --build
```
При запуске автоматически будут подняты все контейнеры, включая основные: fraud_detector database_logger и UI

## Использование
После удачного запуска для работы с сервисом можно воспользоваться следующими UI:
- Streamlit151: http://localhost:8501

- Kafka UI: http://localhost:8080

Kafka UI включает стндартные возомжности по просмотру Топиков и проверки работоспособности отправки сообщений. В Streamlit можно:

Возможности UI
🚀 Отправка

- загружать CSV-файла с транзакциями
- Ограничвать кол-во отправки строк из файла
- Настраивать задержку между сообщениями
- Менять топик отправки (На текущий момент система осталньых модулей мониторит только transactions, так что по сути отправка по другим топикам записываться в БД не будет)
- В графе Show results можно просмотреть гистограмму распределения для последних 100 транзакций и fraud транзакции. Также можно обновить список
